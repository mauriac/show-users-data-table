name: Docker Playwright Tests

on:
  # push:

jobs:
  ptest:
    runs-on: ubuntu-20.04

    # services:
      # mysql:
      #   image: mysql:5.7
      #   env:
      #     MYSQL_ROOT_PASSWORD: root
      #     MYSQL_DATABASE: wordpress
      #   options:
      #     --health-cmd="mysqladmin ping --silent"
      #     --health-interval=10s
      #     --health-timeout=5s
      #     --health-retries=3

      # wordpress:
      #   image: wordpress:latest
      #   ports:
      #     - 8000:80
      #   env:
      #     WORDPRESS_DB_HOST: mysql
      #     WORDPRESS_DB_USER: root
      #     WORDPRESS_DB_PASSWORD: root
      #     WORDPRESS_DB_NAME: wordpress
      #   options: >-
      #     --health-cmd="curl --fail http://localhost:8000/wp-admin/install.php?step=1 || exit 1"
      #     --health-interval=10s
      #     --health-timeout=5s
      #     --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Compose
      run: |
        docker compose up -d
        docker compose ps

    - name: Run Playwright tests
      run: ./run-playwright.sh
